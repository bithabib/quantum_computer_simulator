# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
venv/
.venv/
env/

# Secrets / credentials (do NOT commit)
*.pem
data_storage/*.pem

# Datasets: the CSVs are committed for full reproducibility. Ignore only the
# transient generator log and any large binary/parquet dumps.
data_bp/*.log
*.parquet

# OS / editor
.DS_Store
.vscode/
.idea/
